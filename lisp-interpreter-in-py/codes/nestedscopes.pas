PROGRAM Main;
   VAR x, y: REAL;

   PROCEDURE Alpha(a : INTEGER);
      VAR y : INTEGER;
   BEGIN
      x := a + x + y;
   END;
   
   procedure AlphaB(a : integer);
      var b : integer;
   begin { AlphaB }

   end;  { AlphaB }

BEGIN { Main }

END.  { Main }
